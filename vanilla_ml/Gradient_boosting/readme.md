# Gradient Boosting: Классификация съедобных и ядовитых грибов

В этом ноутбуке демонстрируется применение градиентного бустинга (Gradient Boosting) для задачи бинарной классификации: определения, является ли гриб съедобным или ядовитым.

## 📁 Данные

Используется датасет [Mushroom](https://archive.ics.uci.edu/ml/datasets/Mushroom) из UCI Machine Learning Repository.  
Данные содержат категориальные признаки, описывающие 23 характеристики грибов (форма шляпки, цвет, запах и т.д.). Целевая переменная — `class`:  
- `e` (edible) — съедобный  
- `p` (poisonous) — ядовитый

## 🧪 Что реализовано

- Предобработка данных: one-hot encoding категориальных признаков.
- Обучение модели `GradientBoostingClassifier` из библиотеки `scikit-learn`.
- Подбор гиперпараметров (`n_estimators`, `max_depth`) с помощью `GridSearchCV`.
- Оценка качества модели через `classification_report`.
- Анализ важности признаков (feature importance) и визуализация наиболее значимых из них.

## 📊 Результаты

- Модель достигает **100% точности** на тестовой выборке.
- Наиболее важными признаками оказались:
  - **Запах (`odor_n`)** — отсутствие запаха сильно коррелирует с съедобностью.
  - **Форма основания ножки (`stalk-root_c`, `stalk-root_r`)**.
  - **Наличие синяков (`bruises_t`)**.

> ⚠️ Высокая точность объясняется тем, что в исходном датасете отсутствуют пересекающиеся комбинации признаков для разных классов — это делает задачу почти детерминированной. Тем не менее, пример отлично подходит для демонстрации workflow построения и анализа моделей на основе градиентного бустинга.

## 🛠 Технологии

- Python
- scikit-learn (`GradientBoostingClassifier`, `GridSearchCV`)
- pandas, NumPy
- Matplotlib, Seaborn

## 💡 Зачем это нужно?

Этот проект — часть подготовки к позиции стажёра в области ML/Data Science. Он показывает умение:
- работать с табличными данными,
- обучать и настраивать классические ML-модели,
- интерпретировать результаты,
- оформлять код и анализ в виде воспроизводимого Jupyter Notebook.

---

*Проект создан в рамках практики по машинному обучению. Автор: Ильгиз Магадиев.*